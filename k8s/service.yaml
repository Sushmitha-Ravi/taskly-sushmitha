apiVersion: v1
kind: Service
metadata:
  name: taskly-api
  namespace: default
  labels:
    app: taskly-api
spec:
  type: LoadBalancer
  selector:
    app: taskly-api
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
